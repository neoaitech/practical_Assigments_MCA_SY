pipeline {
    agent any

    stages {

        stage('Build') {
            steps {
                echo 'Build Successful'
            }
        }

        stage('Test') {
            steps {
                echo 'Testing Successful'
            }
        }

        stage('Docker Build') {
            steps {
                sh 'docker build -t attendance-system .'
            }
        }

        stage('Docker Run') {
            steps {
                sh 'docker rm -f attendance || true'
                sh 'docker run -d -p 8081:80 --name attendance attendance-system'
            }
        }
    }
}